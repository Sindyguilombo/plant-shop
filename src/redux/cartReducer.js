// src/redux/cartReducer.js

const initialState = {
  // estado inicial aquí
};

const cartReducer = (state = initialState, action) => {
  switch (action.type) {
    // casos aquí
    default:
      return state;
  }
};

export default cartReducer;
